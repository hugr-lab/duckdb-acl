#
# This workflow calls the main distribution pipeline from DuckDB to build, test and (optionally) release the extension
#
# NOTE: this extension currently tracks duckdb `main` (see README/DESIGN) because it depends on parser/AST
# APIs not yet in a stable release. The distribution build is therefore pinned to `main`; switch these to a
# stable tag once those APIs land in a release.
#
name: Main Extension Distribution Pipeline
on:
  push:
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.head_ref || '' }}-${{ github.base_ref || '' }}-${{ github.ref != 'refs/heads/main' && github.sha || '' }}
  cancel-in-progress: true

jobs:
  duckdb-main-build:
    name: Build extension binaries
    uses: duckdb/extension-ci-tools/.github/workflows/_extension_distribution.yml@main
    with:
      duckdb_version: main
      ci_tools_version: main
      extension_name: acl
