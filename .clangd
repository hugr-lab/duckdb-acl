# clangd reads the compile database the build already maintains (regenerated by every
# `GEN=ninja make`); it covers every src/ translation unit with the full include set.
CompileFlags:
  CompilationDatabase: build/release
